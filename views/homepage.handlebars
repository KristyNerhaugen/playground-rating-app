<div class="flex flex-col md:flex-row mx-5 mt-5">
  <h2 class="text-xl font-semibold">Latest Reviews</h2>

  <ul>
    {{#each posts as |post|}}
      <li>
        {{> post-info post }}
      </li>
    {{/each}}
  </ul>
</div>