<div class="flex flex-col mx-5 mt-5">
  <h2 class="text-xl font-semibold">Latest Reviews</h2>

  <ul class="mt-5 md:w-full">
    {{#each posts as |post|}}
      <li class="mx-5 mb-5">
        {{> post-info post }}
      </li>
    {{/each}}
  </ul>
</div>